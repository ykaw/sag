#!/bin/sh

cd $HOME/.sag || exit 1

cmdseq='/usr/bin/nice /bin/sh bin/t0001'
for sh in bin/t[0-9][0-9][0-9][0-9]
do
    if [ -x $sh ]
    then
        cmdseq="$cmdseq; /usr/bin/nice /bin/sh $sh"
        chmod a-x $sh
    fi
done

export DAYTIME=`date '+%Y %m %d %H %M'`
eval $cmdseq&
