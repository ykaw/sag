#!/bin/sh

cd $HOME/.sag/var || exit

for target in 0001la 0001ps 0001net 0005mem 0100df
do
    (cat sumold-$target;  ../plot/gen$target.pl rl${target%%[a-z]*}) > sumtmp-$target
    ../bin/gnuplot ../plot/gen$target.gp
    # rm sumtmp-$target
done

